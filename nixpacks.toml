[phases.setup]
nixPkgs = ["ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip install yt-dlp --break-system-packages || pip install yt-dlp"
]

[start]
cmd = "node server.js"