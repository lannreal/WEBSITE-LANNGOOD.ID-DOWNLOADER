[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "python3.11 -m pip install yt-dlp",
  "yt-dlp --version || python3.11 -m yt_dlp --version"
]

[start]
cmd = "node server.js"